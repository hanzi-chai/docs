# 汉字自动拆分系统文档

本文档使用 Astro 的 Starlight 主题构建。用户文档和开发文档使用 Markdown 文件编写，而 API 文档使用 `typedoc` 从 TypeScript 源码中自动提取。

## 开发

`npm run dev` 在 `localhost:4321` 启动开发服务器。

## 构建

`npm run build` 在 `dist/` 目录生成静态文件。
